py3:
  name: "Python 3"

  default_run_limits:
    wall_time_s: 9
    memory_kb: 102400
    max_processes: 100

  extension: ".py"

  version_command:
    command: "python3"
    args:
      - "--version"

  run:
    - "python3"
    - "{source}"

c:
  name: "C"

  default_run_limits:
    wall_time_s: 9
    memory_kb: 102400
    max_processes: 100

  extension: ".c"

  version_command:
    command: "gcc"
    args:
      - "--version"

  compile:
    - "gcc"
    - "{source}"
    - "-o"
    - "{artifact}"

  run:
    - "{artifact}"

cpp:
  name: "C++"

  default_run_limits:
    wall_time_s: 9
    memory_kb: 102400
    max_processes: 100

  extension: ".cpp"

  version_command:
    command: "g++"
    args:
      - "--version"

  compile:
    - "g++"
    - "{source}"
    - "-o"
    - "{artifact}"

  run:
    - "{artifact}"

java:
  name: "Java"

  default_run_limits:
    wall_time_s: 9
    memory_kb: 102400
    max_processes: 100

  extension: ".java"

  version_command:
    command: "javac"
    args:
      - "-version"

  compile:
    - "javac"
    - "{source}"

  run:
    - "java"
    - "-cp"
    - "{workspace}"
    - "Main"

bash:
  name: "Bash"

  default_run_limits:
    wall_time_s: 9
    memory_kb: 102400
    max_processes: 100

  extension: ".sh"

  version_command:
    command: "bash"
    args:
      - "--version"

  run:
    - "bash"
    - "{source}"

js:
  name: "JavaScript (Node)"

  default_run_limits:
    wall_time_s: 9
    memory_kb: 102400
    max_processes: 100

  extension: ".js"

  version_command:
    command: "node"
    args:
      - "--version"

  run:
    - "node"
    - "{source}"

verilog:
  name: "Verilog"

  default_run_limits:
    wall_time_s: 9
    memory_kb: 102400
    max_processes: 100

  extension: ".v"

  version_command:
    command: "iverilog"
    args:
      - "-V"

  compile:
    - "iverilog"
    - "{source}"
    - "-o"
    - "{artifact}"

  run:
    - "vvp"
    - "{artifact}"